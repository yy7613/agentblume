# CHANGELOG

agentblume の主要な変更履歴。**利用者にとって意味のある単位**でまとめており、内部リファクタリング・テスト追加・依存更新は原則として省略する。

まだ正式リリース版は無い（`0.1.0` / プレビュー）。日付は変更が入った日。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準じ、バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に従う予定。

---

## [未リリース]

### 修正 — マニュアルを実機で書いたときに見つかった不具合を直した（2026-09-23）

- **設計アシスタントが書いた説明文が、保存で黙って消えていた**: 「ツール名」が空のまま保存すると、エージェント向けの説明文が捨てられていた。片方だけ入っていれば保存し、空の側は補う（名前は公開名、説明は既定の文）。
- **設計アシスタントの返答と実際の変更の食い違い**: 返答の文は返した操作だけを述べ、頼まれていないキー・列・ノードは足さない規則を指示文に足した（実機で「フィルタを挿入しました」と言いながら引数だけ足す症状が消えた）。適用できなかったターンは赤枠の先頭に「適用していません」と出す。
- **足したノードが画面の外に置かれる**ことがあった。足したノードが見えるよう表示を合わせる。
- **日本語の画面に残っていた英語**を日本語にした: 設計アシスタントの変更一覧、キャンバスのノードのカード、テンプレートの必須欄の案内、Factory の計画承認とレポート（候補は表示名で出す）、検証画面（「審査ルーブリック」・版の種類・状態・品質ゲートの合否など）、ツール検証、チャットのトークン表記。
- **チャットで Markdown の表が表として描かれる**ようにした（HTML はそのまま文字として出し、埋め込まない）。
- **設定画面の「安全ゲート」**が、実装済みの認可と監査ログを「未実装」と出していたのを現状に合わせた。
- 設計アシスタントのモデル未設定時の案内を、関数電卓用の文から専用の文と「設定画面を開く」ボタンに変えた。

### 変更 — 所有者の入力を必須から外した（2026-09-23）

- ツール・スキル・エージェント・マルチエージェント・検証の各画面で、所有者（owner）を空欄のまま保存できるようにした。所有者は表示用のラベルで権限の判定には使っておらず、必須にしても入力の手間が増えるだけだったため。
- 空欄で保存すると、サーバーがログイン中の利用者名（単一利用者モードでは `Local operator`）を入れる。入力した値はそのまま保存する。API でも `owner` を省略できる（[docs/04 §5](docs/04-api-spec.md)）。
- テンプレートから作成した直後の案内を「所有者を設定して保存してください」から「内容を確かめて保存してください」に変えた。

### 修正 — 結合キーの候補が無いとき、正しいキーまで外させていたのを直した（2026-09-23）

- Factory がツールの結合を検査するとき、データの下調べで結合キーの候補が見つからず、かつ `注記` のような自由記述列をキーにしていると、`地域コード` のような正しいキーまで「外せ」と差し戻していた。候補が無いときは自由記述列だけを外させる。

### 追加 — 自動生成したツールのノードを、画面幅で折り返して見やすく並べるようにした（2026-09-23）

- テンプレートや設計アシスタントで作ったツールが横一列に長く並び（12 ノードで約 2,700px）、全体を収めると文字が読めなかった。上流からの段数で列を決め、**4 列を超えたら下の帯へ折り返す**配置にした。どこにもつながらない引数ノードは最上段にまとめる。
- テンプレートから作成したとき・位置の無いツールを開いたとき・空のキャンバスへ設計アシスタントがノードを置いたときに自動で使う。手で動かした位置は変えない。
- キャンバス左上に「**整列**」ボタンを足した。いまの表示幅に合わせた列数で並べ直し、「元に戻す」で整列前へ 1 回戻せる（[docs/06 §3.18](docs/06-etl-tool-builder.md)）。

### 変更 — 設計アシスタントの理由と注記を日本語で出すようにした（2026-09-23）

- 設計アシスタントが返す赤枠の理由（存在しないノードへの操作・期間の文字列での並べ替え・0 行・結合の規則違反 など）と黄色の注記が、日本語の画面でも英語のまま出ていた。約 30 の定型文に訳を足し、列名・ノード名を埋め戻して、何をどこで直せばよいかを日本語で示す。
- 設計アシスタントで結合を組むときも、Factory と同じ結合の規則（期間の解釈は最後の結合の後に 1 回・注記の列をキーにしない）で検査するようにした。

### 追加 — 設計アシスタントに、文脈の消費メーター・履歴の圧縮とクリア・Tool Calling 契約の説明の更新を足した（2026-09-23）

- **どれだけ文脈を使っているかが見える**: パネルの見出しの下に「文脈 8.6k / 200k（4%）」のメーターが出る。値は推定ではなく、直前の応答でモデルが数えたトークン数と、LM Studio から取れる読み込み中モデルの文脈の長さ。取れなければトークン数だけ出す。70% で黄、90% で赤になり、圧縮かクリアを促す。
- **履歴を圧縮できる**: 会話が 5 ターン以上になると「履歴を圧縮」が押せる。直近 4 ターンを残し、古いターンをモデルに「次のターンで読む覚え書き」（目的・決めたこと・適用した変更・未解決の質問）に要約させる。圧縮したターンの変更は取り消せなくなる。
- **履歴をクリアできる**: 会話を消してもキャンバスは残る。
- **エージェント向けの説明文を会話で更新できる**: 「説明文に引数の書式と粒度の綴りを書いて」で、ツールの説明（エージェントが呼ぶ前に読む文）が書き換わる。引数を足したり変えたりしたときは、頼まなくても同じ応答で説明文が更新される（説明が古いままだとエージェントが誤った値を渡すため）。
- **API**: `POST /tool-drafts/design-chat/compact` を追加。`POST /tool-drafts/design-chat` の応答に `usage`・`agentTool`、要求に `agentTool`・`transcriptSummary` を追加した（[docs/04 §3.9](docs/04-api-spec.md)）。

### 追加 — ツール作成画面に、文章で指示するとノードが増える設計アシスタントを足した（2026-09-22）

ツール作成画面のヘッダーに「**設計アシスタント**」ボタンを足した。押すと右側にチャット欄が開き、Claude Code や Codex のように「都道府県別の人口を年次に絞って多い順に10件返して」のような日本語の指示を書くと、話しながらキャンバスにノードが増えたり設定が変わったりする。空のキャンバスから組み始めることも、組んだ後の手直し（「地域を引数で絞れるように」）もこの1つでできる。

- **確認なしで即キャンバスへ反映し、気に入らなければ1手で戻せる**: 毎回の確認ダイアログを挟まない代わりに、各返答に「この変更を取り消す」を付けた。取り消すとそのターン（とそれ以降）の変更前へ戻り、会話は残る。
- **指示どおりに直せなかったときは、キャンバスを壊さず理由を返す**: 一度だけ内部で出し直しを試み、それでも通らなければキャンバスには触れずに赤い枠で理由を出す。
- **答えが間違う形は止める、意図次第の形は注記で伝える**: 期間の文字列で並べ替える（「2025年9月」が「2025年12月」より後になる）・プレビューが 0 行になる、は直るまで適用しない。月次と年次が混ざる形は 1 回だけ直させ、直らなければ黄色の注記つきで適用する（「全行を」のように意図的なこともあるため）。
- **小さなモデルの書き間違いは自動で直す**: ノード名の `_` と `-` の揺れ、結合キーの配列書き、引数に束縛した条件の空の値、出力ノードの必須項目の書き忘れなど、実測で差し戻しの大半を占めていた機械的なミスは、出し直させずにその場で直す。
- **保存はしない**: これまでどおり、内容を確かめてから「バージョンを保存」で保存する。
- モデル未設定のときはボタンは押せるが入力欄が無効になり、設定箇所を案内する（関数電卓の「AIに式を書かせる」と同じ条件・同じ文言）。
- **API**: `POST /tool-drafts/design-chat` を追加、`GET /runtime/capabilities` に `designAssistant` を足した（[docs/04 §3.9](docs/04-api-spec.md)）。

### 変更 — モデルへ送る指示文を外部ファイル `prompts/` で持つようにした（2026-09-22）

Factoryの各役割・関数電卓やAI判定の提案・検証のアンケート・仕訳や経費精算の読み取りなど、モデルへ送る指示文がコードの中に埋め込まれていたため、文を直すたびにビルドが必要で、どこにどんな指示があるかも見通せなかった。**ツールテンプレートと同じ考え方**で、指示文を `prompts/**/*.md` の外部ファイルへ出した。

- **文の変更に再起動は要らない**: ファイルの更新時刻を見て次に使うときに読み直す。運用者が自分のモデル（12B / 27B / クラウド）に合わせて言い回しを調整できる。
- **環境変数 `AGENTCONTEXT_PROMPTS_DIR` で自分の置き場所を足せる**: ツールテンプレートと同じ `;` / `:` 区切り、同じ「同じidは後勝ち」の流儀。
- **欠けや壊れには、テンプレートより厳しく振る舞う**: 起動時にコードが必要とする文と節が全部揃っているかを検査し、1つでも欠けていればどのファイルの何が無いかを出して**起動を止める**。実行中に壊れたファイルを読み込んだときは直前の良い版を使い続け、警告だけを残す（動いているサーバーの指示が消えないように）。
- 組み立て（どの節をどの順に使うか・条件での入れ替え・利用者データの隔離・出力形式の指定）はこれまでどおりコードが持ち、ファイルには文だけを置く。挙動は変えていない（移行は一字一句そのままの置き換え）。

### 追加 — ツールをテンプレートから作れるようにした（外部ファイル。Factory とツール作成画面で共通）（2026-09-20）

前年比・期間内の統計・相関・2 つのデータの比といった「作れるはずなのに誰も作れていなかった」形を、**テンプレート**として外部ファイル（JSON）に用意した。実測では、決定的に計算できる部品があるのにエージェントが前年比を暗算していた（[ADR-0047](docs/adr/0047-factory-lessons-from-estat.md)）。構成をテンプレートが持てば、手元の小さなモデルでも人でも「選んで埋める」だけで同じツールが作れる。経緯は [ADR-0049](docs/adr/0049-tool-templates.md)。

- **Factory はまずテンプレートを試す**: 新しいツールを作るとき、そのデータで使えるテンプレートの中から AI に 1 つ選ばせ、スロット（データソース・列・少数の選択肢）を埋めさせるだけでツールができる。AI への質問は 2 回だけで、列名や結合キーは実データから作った選択肢の中からしか選べない。合うテンプレートが無ければ「使わない」と答えてよく、その場合は従来の作り方（段階的な生成 → 一括生成）へ自動で落ちる。
- **前年比・統計・相関・比率を「ツールが計算した列」として返せる**: 同梱のテンプレートから作ったツールは、増減・増減率・平均・最大・最小・相関係数・一人当たりの値などを決定的に計算して返す。エージェントには「計算済みの列はそのまま引き写し、自分で計算し直さない」規律が自動で入る。
- **どのテンプレートから作ったかが記録に残る**: 実行の記録に、使ったテンプレートの id と版、埋めたスロットの値が残る。検査に落ちたときはスロットを 1 回だけ選び直し、それでも駄目なら理由（どのテンプレートが何で駄目だったか）を残して次の作り方へ進む。
- **計画の段階からテンプレートを意識する**: 計画役にも「このデータで使えるテンプレート」の一覧を渡し、テンプレートで作れる形のツールを優先して計画するようにした。
- **テンプレートは足せる**: `templates/tools/` に JSON を置けばコードを変えずに構成を追加でき、環境変数で自分の置き場所も足せる（同じ id は後勝ち）。再起動は要らない。壊れたファイルは読み飛ばし、理由と直し方を一覧に出す。
- **ツール作成画面に「テンプレートから作成」を足した**: ツール一覧の「新規作成」の隣。テンプレートを選ぶ（タイトル・要約・使いどころ・タグ・要るデータソースの数と絞り込み欄）→ データソースを選ぶ → スロットを埋める → キャンバスへ展開、までを 1 つのダイアログで行う。表示名・公開名・エージェント向けの名前と説明も埋まるので、所有者を入れれば保存できる。
- **選ぶ根拠を選択肢の横に出す**: 列は型、カテゴリの列は実際に入っている値の例、期間の列は粒度ごとの行数と日付の範囲、結合キーは値の重なりを添える。結合キーを選び残したときと、選んだキーで行が一意にならないときは警告を出す（結合で行が増えることに、作った後ではなく選ぶ時点で気づける）。
- **合わない選択はその欄の真下で指摘する**: 「列『世帯数』はこのデータソースの数値の列ではありません。人口 から選んでください」のように、何が悪いかと何を選べばよいかを日本語で出す。直し始めるとその指摘は消える。
- **式を AI に書かせるテンプレート**では、作成後に該当の関数電卓ノードが選択された状態になり、「AIに式を書かせる」に「何を計算するか」が入った状態で開く（人は式を書かなくてよい）。
- **読み込めなかったテンプレートを画面で見られる**: 一覧の下の「読み込めなかったテンプレート」（既定は畳んである）に、ファイル名と理由・直し方が出る。直してダイアログを開き直せば反映される。
- **API**: `GET /tool-templates`（一覧と読めなかったファイル）、`POST /tool-templates/:id/slot-candidates`（候補）、`POST /tool-templates/:id/instantiate`（実体化。保存はしない）を追加した（[docs/04 §3.8](docs/04-api-spec.md)）。

### 変更 — 結合の行数上限を変えられるように / テンプレートで引数の必須・任意を選べるように / チェックボックスを横並びに統一（2026-09-22）

- **結合の「最大行数」を設定できるようにした**: 結合は出力が10万行を超えると止まる作りで、この上限が固定だったため、キーが正しくても10万行を超える結合（都道府県 × 月次 × 複数指標など）が作れなかった。結合ノードの設定に「最大行数」（既定 100,000、最大 10,000,000）を足した。既存のツールは10万行のまま変わらない。
- **サーバー全体の実行上限も変えられるようにした**: 1つのノードが作ってよい行数の上限（既定 250,000 行）を、環境変数 `AGENTCONTEXT_MAX_EXECUTION_ROWS` で変えられる。結合の上限だけ上げてもこちらで止まっていた。
- **止まったときに、どこを上げればよいかを言う**: 「結合キーを確認し、正しければこの結合ノードの『最大行数』を上げてください」「上流で行を絞るか、環境変数 AGENTCONTEXT_MAX_EXECUTION_ROWS を上げてください」と、当たった上限に応じて直し方を出す。
- **テンプレートから作るとき、引数ごとに「必須」を選べるようにした**: 「テンプレートから作成」に「エージェントの引数」の区画を足した。地域を毎回必ず指定させる、といった調整を作る時点でできる。「必須」を外した引数は、エージェントが省略するとその条件で絞らない。
- **切り替えると壊れる引数は固定する**: 粒度（月次・年次など）は、任意にすると省略されたときに月次と年次が混ざるので、同梱のテンプレートでは必須に固定し、画面に理由を出す。テンプレート作者は `lock` に理由を書けば、どの引数でも固定できる。
- **チェックボックスとラベルを横並びに統一した**: 画面によってチェックボックスが文言の上に縦に積まれていた（テンプレートの列・結合キーの選択、設定画面、経費精算の規程など）。すべて「☐ 文言」の横並びにそろえた。
- **API**: `POST /tool-templates/:id/instantiate` に任意の `argumentNullability`、`slot-candidates` の応答に残る引数の一覧 `arguments` を足した（[docs/04 §3.8](docs/04-api-spec.md)）。

### 変更 — テンプレートからツールを作るとき、名前を必ず決めるようにした（2026-09-21）

これまでは名前を省略でき、省略するとテンプレートの名前（`period-series` など）がそのまま内部ID・公開名・関数名になっていた。そのため同じテンプレートから2本目を作ると1本目と同じ内部IDになり、**別のツールのつもりで保存すると既存のツールの新しいバージョンになってしまう**（人口の推移と賃金の推移を同じテンプレートから作る、という普通の使い方で起きる）。

- **「テンプレートから作成」で「ツール名」と「関数名」を必須にした**: どちらも空から入力する（テンプレートの名前は初期値に入れない）。ツール名は一覧に出る表示名で日本語でよい。関数名はモデルがツールを選ぶときに見る名前で、英数字・`_`・`-` の1〜64文字。
- **既にあるツールと同じ関数名は、その場で指摘する**: 保存まで待たずに「この関数名はツール『…』が使っています」と欄の下に出す。
- **API**: `POST /tool-templates/:id/instantiate` の `toolName` を必須にした（[docs/04 §3.8](docs/04-api-spec.md)）。

### 変更 — Factory の検証シナリオに「相手が持つデータ」を伝えるようにした（2026-09-21）

ツールもエージェントも正しく動いているのに、品質の判定が「目標未達」のままになる原因が検証する側にあった。擬似ユーザーは人物設定と目標の 1 行しか知らされておらず、「総支給額 320,000円・165時間で時間当たり給与を出して」と自分で数値を作って渡したり、データに無い指標を尋ねたり、正しい回答を自作の試算と比べて誤りだと言い張ったりしていた。

- **検証の前提をシナリオの「状況」に自動で付ける**: Factory が作るシナリオには、相手のエージェントが持つデータ（名前・値の列・期間の範囲と粒度・カテゴリの例）と、「自分で数値を作って渡さない」「範囲内で質問する」「データの値つきの回答を自作の試算と比べない」という前提が入る。データの実測から決定的に作るので、毎回同じ内容になる。Scenario 画面で読めて、直せる。
- **データの期間外の年を名指しするシナリオは計画役に 1 回だけ直させる**: 直らなくても Run は止めない（前提が実際の範囲を伝える）。
- **ペルソナには「ユーザーがどんな人か」だけを書かせる**: エージェント向けの指示（「必ずツールの出力を使うこと」）がペルソナに入り、擬似ユーザーが監督者のように振る舞っていた。
- **答えが得られたら検証の会話を終える**: 1 ターン目で答えを得た後も、擬似ユーザーが要因分析や別の比較を求め続けて上限ターンに達し、「目標未達」になっていた。前提に「目標に書かれた内容に答えが得られたら達成とし、目標に無い追加の分析で会話を延ばさない」を足した。
- **アンケートの評点の向きを明示する（全シナリオ共通）**: 擬似ユーザーが自由記述で「正確で明瞭」と書きながら満足度 1 を付けていた。設問の説明が「総合満足度（1〜5）」だけで、どちらが高評価かを書いていなかった。説明に「1 = 最低、5 = 最高」を入れ、「自由記述と評点を一致させる」と指示する。既定の設問「手間の少なさ」の言い回しも、向きが逆に読めないように直した。

e-Stat の実データとローカル 12B で 1 パターンにつき 1 回ずつ回し直した結果、前年同月比と時間当たり給与の 2 パターンとも、品質の判定が「目標未達」から「目標達成」になった（達成率 1.0、満足度 5.0 / 4.0）。1 周目で目標を満たすので、1 回の生成にかかる時間も約 20 分から 4〜6 分になった。

経緯は [ADR-0050](docs/adr/0050-scenario-grounding.md)、契約は [implementation/v44](implementation/v44-scenario-grounding.md)。

### 修正 — テンプレート経路を e-Stat の実データで回して見つけた 3 件（2026-09-20）

- **計画役が結合先を書き忘れても、2 つのデータを使うツールになる**: 計画の文章に「現金給与総額 ÷ 総実労働時間」と書きながら結合先のデータソースを指定し忘れ、給与総額だけを返すツールができて、エージェントが給与総額を「円/時間」と答えていた。計画の文章が別のデータソースにしか無い列を名指ししていれば、そのデータソースを結合先として自動で補う（結合できる相手だけ）。
- **ほぼ空の列を結合キーの候補に出さない**: e-Stat の「注記」のように空の値がある列をキーにすると、空同士は一致しないため行が黙って落ちる（167 か月のうち 1 か月しか残らなかった）。空の値を持つ列は候補から外した。Factory とツール作成画面の「テンプレートから作成」の両方に効く。
- **テンプレートの引数の説明がエージェントに届く**: テンプレートに書いた引数の説明（粒度の綴り・日付の渡し方）が、作られたツールの説明文に入っていなかった。そのためエージェントが粒度に `monthly` を渡して「データが無い」と答えていた（正しくは `month`）。引数の説明をツールの説明文の末尾へ「引数:」として入れるようにし、粒度の説明は全テンプレートで「この綴りのまま渡す」に揃えた。

経緯は [ADR-0047 第6ラウンド](docs/adr/0047-factory-lessons-from-estat.md)。

### 変更 — Factory のツール生成を、小さな目的別タスクと決定的な組み立てに分けた（2026-09-20）

これまで Factory は「ツールの設計図（グラフ）全体」を 1 回の指示で AI に書かせていた。手元の小さなモデルでは試すたびに別の書き間違いが出て、1 回 2〜5 分を使ったあげくツールが 1 本もできない実行が続いていた（[ADR-0047](docs/adr/0047-factory-lessons-from-estat.md) の実測）。設計の判断と組み立てを分け、組み立てはコードが担うようにした。経緯は [ADR-0048](docs/adr/0048-staged-tool-generation.md)。

- **AI には「決めること」だけを聞く**: 何で絞るか（期間の粒度・範囲・地域などのカテゴリ）、何を計算したいか、何を返すか（列・並び・件数）を、それぞれ小さな質問に分けて聞く。列名やキーは実データから作った選択肢の中からしか選べないので、存在しない列名を書かれることが構造的に無くなった。
- **ツールの中身はアプリが組み立てる**: ノード・つなぎ方・引数の宣言・説明文は、決まった手順で機械的に作る。同じ判断なら必ず同じツールができ、引数の説明には実在する値の例・データの期間範囲・返す列が自動で入る。月次と年次が混ざるデータでは粒度の指定を必須の引数にして、混ざった数字が返らないようにした。
- **差や比を「ツールが計算した列」として返せるようになった**: 計算が要るときは、式づくりに特化した既存のアシスタント（文法・関数一覧・検証・試算つき）へ別途お願いして列を足す。式が作れなかった計算列だけを取り下げてツール自体は作る。エージェントには「計算済みの列はそのまま引き写し、自分で計算し直さない」規律も足した。
- **失敗したら、原因を決めた担当にだけ聞き直す**: 「結合で行が増えた」「返す行が多すぎる」といった検査の指摘を、それを決めた質問へ理由つきで 1 回だけ差し戻す。全体を書き直させないので、直したつもりが別の場所を壊すことが無くなった。やり直した内容・取り下げた計算列は実行の記録に残る。
- **うまくいかないときは従来どおりの方法に自動で切り替える**: 段階的な生成が失敗したら理由を記録して従来の一括生成へフォールバックする。詳細オプションの「ツールの作り方」で、段階的（推奨）/ 一括 を選ぶこともできる（既定は段階的）。

### 追加 — フィルタで複数の値を一度に指定できるようにした（2026-09-20）

「東京都・大阪府・北海道を比べて」と頼まれたエージェントは、フィルタが1条件につき1つの値しか取れないため県ごとにツールを呼び、1回の実行で許されるツール呼び出しの上限に当たって答えられなくなっていた（実測）。回避策の「地域を指定せず48行全部返す」は文脈を浪費するだけで、県が増えれば破綻する。

- **「いずれかに一致 / いずれにも一致しない」をフィルタに追加**: 1つの条件に値を並べて（最大100件）、一度に絞り込めるようにした。値は画面の入力欄にカンマ・読点・セミコロン・改行のどれで区切っても構わない。入力した値は解析した件数とともに直下に表示する。
- **エージェントは1回の呼び出しで複数の値を渡せる**: 値をAIの引数にすると、エージェントは `東京都,大阪府,北海道` のようにカンマ区切りの1つの引数でまとめて渡せる。ツールの説明文にも渡し方と例が自動で載る。引数を任意（省略可）にしておけば、省略で「その絞り込みをしない」も選べる。
- **0件のときは「どの値が空振りしたか」まで返す**: 指定した値のうち1件も無かったものを名指しし、その列に実在する近い値を添えてエージェントへ差し戻す。一部の値だけが見つかった場合は、見つかった分の行をそのまま返す（作り話をさせないための情報を、実行トレースにも残す）。
- **間違いは直し方つきで止める**: 値が空のまま・日付や数値として読めない値が混ざっている・値の数が多すぎる場合は、どの値が問題かを名指ししたエラーにする（黙って0件を返さない）。

### 追加 — Factory が複数のデータソースを結合するツールを作れるようにした（2026-09-20）

「現金給与総額と労働時間の推移を調べ、同じ時点の賃金と労働時間を並べて説明する」のように、答えが**複数のファイルの値を同じ行に並べる**ものだったとき、これまでの Factory はファイルごとに別々のツールを作っていた。エージェントは各ツールを呼んだうえで行を自分で突き合わせることになり、実測では突き合わせを誤る・そもそもどれも呼ばないという結果になっていた。経緯と判断は [ADR-0047](docs/adr/0047-factory-lessons-from-estat.md) の第3ラウンド。

- **1つのツールで結合する**: 計画役がデータソースをまたぐツールを立てられるようにした（1ツールにつき主データソース + 最大 2 件）。生成されるツールは各ファイルを読んで共通のキー（`時点` と `地域コード` など）で結合し、賃金と労働時間を同じ行に並べて返す。
- **結合できる組み合わせを自動で見つける**: データ把握の段で、ファイル同士の同名・同型で値が十分に重なる列を洗い出し、「そのキーで行が一意に決まるか」まで調べて計画役・ツール作成役へ渡す。一意でないキーは行が増えるため、警告として明示する。
- **結合したツールを決定的に検査する**: 各ファイルをちょうど 1 回ずつ読んでいるか、枝が結合ノード以外で合流していないか、結合キーが左右に実在して型が合うか、結合で行が増えていないか（キー不足の検出）、どのファイルの値も出力に残っているか。違反は「どのノードをどう直すか」を添えてツール作成役へ差し戻す。
- **複数カテゴリを 1 回の呼び出しで頼める**: 地域などのカテゴリ引数を複数値（カンマ区切りの一覧）で受け取る形に統一した。「東京都・大阪府・北海道を比較」がツール呼び出し 1 回で済む（以前は地域ごとに呼んで上限に当たり、会話ごと失敗していた）。生成されるエージェントの規律とツールの説明文にも、渡し方と省略時の挙動を書く。

### 変更 — e-Stat の実データで Agent Factory を回し、見つかった問題を直した（2026-09-20）

政府統計（e-Stat）の時系列 CSV をデータソースに、Factory でエージェントを 3 パターン（単一系列・多地域・複数ソース）生成して結果を調べ、次を直した。経緯と実測値は [ADR-0047](docs/adr/0047-factory-lessons-from-estat.md)。

- **CSV 入力**: 先頭の BOM を落とす（Excel / e-Stat の「UTF-8（BOM 付き）」で 1 列目の名前が不可視文字つきになり、見た目どおりの列名で参照できなかった）。先頭ゼロつきの値を含む列（地域コード `00000` など）は列ごと文字列のまま保つ。
- **「期間の解釈」ノードを追加**（`parse-period`）: `1975年10月`・`2024年1-3月期`・`2024年`・`2024年度`・和暦・`2024-01`・`2024Q1` などの期間ラベルから、期間の開始日と粒度（月・四半期・年・年度）の列を足す。月次と年次が同じ列に混ざるデータでも、粒度で分けて期間で絞り、正しい時系列順に並べられる。
- **フィルタ**: 日付列の条件値に ISO 文字列（`2008-01-01`）を書けるようにした。これまではエラーも出ずに常に 0 件になっていた。読めない値は直し方つきのエラーにする。
- **ツールが 0 件を返したら理由を添える**: どの条件が何件に一致したか、その列に実在する近い値（`2015年12月31日` に対して `2015年`）をエージェントへ返す。0 件のときにエージェントが数値を作り話する問題への対策で、実行トレースにも残る。
- **シナリオ検証**: アンケートの満足度に 1〜5 の範囲をスキーマで課し、範囲外は理由を添えて 1 回だけ直させる。アンケートだけ失敗しても会話の結末（達成・未達）は捨てない。失敗した段（疑似ユーザー / エージェント / アンケート）と理由を記録して画面に出す。
- **Factory の指標と診断**: ツール使用率が常に 0 になる不具合を直した（期待するツール名が内部公開名で、実際に呼ばれる関数名と食い違っていた）。分析役へ、失敗の段と理由・実際のツール呼び出しと引数・返った行数・「0 件なのに数値で答えた」印・前回からの悪化を渡す。提案が 0 件なら理由を添えて 1 回だけ再依頼し、止まった理由を記録する。
- **Factory が作るツール**: 期間の定石（期間の解釈 → 粒度で絞る → 開始日の範囲で絞る → 並べ替え → 件数を縛る）を教え、生成物を決定的に検査する（期間ラベル列と値の列が出力に残っているか、日付引数が date 型か、同じ引数を下限と上限の両方に束縛していないか、引数なしで呼んでも行数上限を超えないか）。データの列ごとの期間の粒度・範囲、種類の少ない列の実在値を計画役とツール作成役へ渡す。
- **生成したエージェントの規律**: 数値はツール結果からだけ答える・0 件ならそう伝えて実在値を案内する・時点と単位を添える、を決定的に合成する部分へ入れ、分析役によるプロンプト書き換えでも消えないようにした。
- **レポートの正直さ**: Run が完走しても、目標未達（`below-targets`）か測れていない（`unverified`）かを理由つきで示す。アンケート未回収の件数も指標に出す。最良イテレーションは達成率 → 会話失敗率 → 満足度の順で選ぶ。
- **計画役**: データソース id を出力スキーマの選択肢に縛り、UUID の写し間違い（実測で 2 回、計画段階で Run ごと失敗）を起こせなくした。

### 変更 — 関数電卓の上で AI に式を書かせられるようにした（2026-09-20）

- 「ローカルLLMで式を作る」区画をダイアログ最上部から、電卓の式の表示欄の直下（「✨ AIに式を書かせる」キー）へ統合した。式を見ながら「自分で組む／AI に書かせる」を選べる。
- ローカル LLM が未設定のときもキーは隠さず、無効化した状態で「ローカルLLMが未設定です。設定 > モデル で main スロットを設定して再読み込みすると、AIに式を書かせられます。」と直し方・直す場所を添える（以前は未設定だと区画自体が出ず、機能に気づけなかった）。
- 「計算したいこと」の指示文を編集しているあいだは、下の「値として使える入力」のキーが式ではなく指示文へ `[列名]` を挿入するようになった。列名を打ち間違えずに指示へ書ける。
- 式が既に書かれているときは、AI がその式を直す（指示文の置き文字も改訂の例に変わる）。プロンプトの版を `calculate-expression/v2` に上げた。

### 変更 — 関数電卓に「値として使える入力」を出した（2026-09-19）

- 関数電卓の設定ダイアログで、式のすぐ下に**上流の列を型と例の値つきのキー**として並べた（押すと `[列名]` がカーソル位置に入る）。何を値として参照できるかを見てから式を組める。
- 例の値はプレビューの先頭から null を除いた相異なる値を 3 つ。プレビューがまだ無いときは型だけを出す。数値でない入力は薄く出し、型変換（cast）を上流に置く案内を添える。
- サイドバーの要約にも「値として使える入力」を `[列名]` で出す。

### 追加 — 関数電卓の式をローカルLLMに提案させられるようにした（2026-09-19）

- 関数電卓（`calculate`）ノードの設定ダイアログに「ローカルLLMで式を作る」区画を追加した。
  「単価×数量の税込金額を小数 0 桁で」のように**やりたいことを日本語で書く**と、上流の列名と標本行から
  検証済みの式を 1 本提案する。式の文法や関数名を覚えていなくても組める。
- **提案は使う前に検証する。** 式として正しいか判定し、誤りがあれば直せる形（種別・候補列）で LLM へ
  1 回だけ差し戻す。通ったら標本行で実際に計算し、全行失敗や数値でない列への依存が無いかを確かめてから返す。
  2 回目も通らなければ、最後の診断と直し方の候補を添えて失敗にする。
- **提案は自動適用・自動保存しない。** 式・根拠・何行計算できたか・警告を確認してから、利用者が
  「この式をダイアログへ適用」を押した時だけ式と出力列名を取り込む。評価できないときの扱い・小数桁は
  現在の設定を保つ（LLM に委ねるのは「何を計算するか」だけ）。
- **この電卓で表せない指示は、それらしい式をでっち上げずに断る。** 文字数を数える・条件で値を変える、のような
  指示には式を作らず、「なぜ書けないか」と「次にやること」（数値の計算に言い換える / 別のノードで前処理する）を
  添えて失敗を返す。入力列を 1 つも使わない式（定数だけ）が提案された場合は拒まないが、警告に出す。
- **分かっている限界**: 「割引率が空の行は割引なしにしたい」のような**空を既定値へ倒す**指示は、式言語に
  `coalesce` 相当が無いため正しく表せない。空の行は結果も空になり、プレビュー要約の
  `N/M 行が計算できません（内訳: missing-value …）` で気づけるようにしてある。
- 有効化条件は分析ノードの設定補助（[§3.12](./docs/06-etl-tool-builder.md#312-ローカルllmによる分析設定補助)）と同じ。
  詳細は [ADR-0046](./docs/adr/0046-calculate-expression-assistant.md)、困ったときは
  [トラブルシューティング](./docs/19-troubleshooting.md#関数電卓の式を提案が失敗する--変な式が出る)を参照。

### 変更 — 関数電卓: 式の誤りを種別で返し、打ち間違いの候補を出すようにした（2026-09-19）

- **打ち間違いに近い候補を出す。** `sqr(` なら `sqrt`、`lgo10(` なら `log10`、列名の `金学` なら `金額` のように、
  1 文字違いの取り違えに「もしかして」を添える。見当違いの候補はかえって迷わせるので、遠いものは出さない。
- **誤りの種類を 21 種に分けた。** 表示される文言は今までどおりだが、内部では「使えない文字」「引数の数が合わない」
  「閉じ括弧がない」といった種別を持つようになった。画面が原因ごとに案内を出し分けられる。
- **式が何を出すかを、実行せずに確かめられるようにした。** 行ごとの結果と、計算できなかった行の理由の内訳
  （0 除算が何行、参照列が空が何行…）をまとめて取れる。最初に失敗した行も分かる。
- 式の判定（列があるか・型が合うか）の規則を 1 か所へ集約した。設定画面の問題一覧と、この判定が食い違わなくなる。
- これらは次の増分（**式の設定を LLM に任せる機能**）の土台。提案が正しいか判定し、誤りを直せる形で差し戻し、
  何を出すか確かめる、の 3 つを機械可読にしたもの。
- **誤りを「直し方が同じもの」でまとめられるようにした。** 列名の打ち間違いは `[列名]` で書いても裸で書いても
  内部の種別は違うが、直し方はどちらも「列名を直す」。この粒度で束ねた分類を持たせ、案内や差し戻しを
  種別ごとにばらけさせない。
- **全行が空になったとき、何を直せばよいかまで言うようにした。** 「文字列の列だから寄せます」という注意だけでは
  上流に型変換が要るか決まらない。実際の行を見て 1 つも数値にならなかった列を特定し、「列 X の値を数値に
  できません。上流に型変換を入れてください」と列名付きで返す。全行が空・全行が 0 除算のときも同様。
  一部の行だけ失敗する場合は案内を出さない（正常な式に毎回「直せ」と言わないため）。

### 追加 — ツール作成に「関数電卓」ノードを足した（2026-09-19）

- ツールビルダーに **関数電卓（`calculate`）ノード**を追加した。入力列を使った数式で新しい列を計算する。
  「税込金額 = 金額 × 1.1」「単価 = 金額 ÷ 数量」のような表計算のセルに書く程度の計算が、ノーコードで組める。
- **設定画面は関数電卓の見た目。** 数字と演算子のキーパッド、4 群の関数ボタン（基本 / 丸め / 指数・対数 / 三角）、
  入力列のボタン（押すと `[列名]` が入る）で式を組む。式は直接編集もでき、ボタンはカーソル位置へ挿入する。
- **評価できない行は空にして続ける**（既定）。0 除算や負の平方根、参照列が空のときも行を落とさない。
  1 行でも止めたいときは「評価できないとき: fail」を選ぶと、行番号・式・理由を添えて止まる。
- **止まったときの理由は原因ごとに変わり、直す先まで出す。** 「0 で割っています。分母が 0 になる行を
  上流の行フィルターで除くか…」のように、0 除算・参照列の欠損・定義域の外・桁あふれ・丸めの桁数を
  区別する。どれも結果は空になるが、直す場所が違うため。
- **打ち間違いは実行前に分かる。** 上流に無い列名や未知の関数名は、設定画面の問題一覧に出る。黙って 0 や空にはしない。
  関数名の綴り違い（`sqr(`）、括弧の書き忘れ（`sqrt 4`）、表計算ソフトの癖（先頭の `=`、`SUM(`、桁区切りの `1,234`）、
  演算子の書き忘れ（`2(3+4)`、`2 x 3`）は、それぞれ原因を言い当てた文言と式の中の位置を返す。
  `max(1,)` のように区切りのあとに引数が無い場合、以前は「対応する ( がありません」と括弧の不一致を疑わせていたが、
  引数の不足として伝えるようにした。
- 式の解釈は自前の小さな解釈器で行い、`eval` は使わない。比較・条件分岐・文字列は受け付けない（行の絞り込みは
  フィルタと AI 判定の役目）。

### 追加 — ツール作成に「AI判定」ノードを足した（2026-09-18）

- ツールビルダーに **AI判定（`ai-judge`）ノード**を追加した。「この問い合わせはクレームですか？」のような日本語の判定基準を書くと、各行を読んで**はい/いいえ**、または自分で決めたカテゴリ（最大20件）に振り分ける。DifyでいうLLM条件分岐・質問分類器に近い機能。
- 判定列を足して全行を通す（**flag**）→ 後続の**フィルタ**ノードで判定結果に応じて絞り込む、という組み合わせで「AIの判断による分岐」を表せる。よくある「判定してすぐ絞り込む」用途向けに、判定した行だけを残す・除く（**keep** / **exclude**）のショートカットも用意した。
- **判断できなかった行は「不明」のまま扱い、フィルタ条件へ含めない限り黙って通過・除外しない。** AIの不確かな答えが気づかないうちに結果へ混ざらないようにする安全側の既定。
- 1回の実行で判定できる行数には上限（既定50件、最大200件）があり、超えると「上流を絞り込んでください」という案内付きで止まる。同じ内容の行は1回しか判定しないので、重複の多い表では上限に達しにくい。
- モデルが未設定・判定に対応していないときは、ノードの場所と「設定 > モデル」への導線を添えて止まる。詳細は [docs/06-etl-tool-builder.md §3.13](docs/06-etl-tool-builder.md#313-ai-判定ノード判断条件に-ai-を使う)（設計判断は [ADR-0044](docs/adr/0044-ai-judge-node.md)）。

### 追加 — ツール検証で「AI判定」の答えを確かめられるようにした（2026-09-18）

- ツール検証の期待に、**行を特定した期待**（`id が E1 の行の金額 >= 10000` のように、終端出力の1行を列＝値で特定してセル条件を確かめる）を追加した。
- **AI判定ノードへの期待**も追加した: `ai-judge` ノードの入力行を特定し、「この行は no と判定されたか」を直接確かめられる。判定した行が `keep` / `exclude` で終端出力から消えていても、ノードの判定結果そのものを検証できる。AIの答えの揺れを許容するため、期待する判定値は複数（いずれか一致すれば合格）書ける。
- 結果には判定表（各AI判定ノードの入力行 + 判定 + 理由のスナップショット）と、判定に使ったモデルを記録するようにした。ケースが不合格になったとき、判定基準そのものの回帰かモデルの入れ替えによる揺れかをその場で切り分けられる。

### 追加 — 経費精算を実用的にした（2026-09-15）

- 経費精算に **9 つのユースケース**を足した: 従業員マスタ（CSV 取込・部門・承認者グループ）、多段承認（費目・金額・部門で経路を選ぶ。上長・部門長・グループ・代理）、**全銀協形式の振込データ**（事前点検 → 作成 → 再ダウンロード → 確定）、仮払金（申請 → 承認 → 支払 → 申請との精算）、法人カード明細の取込と申請との照合（二重計上の疑いを止める）、月次の集計（部門・費目・申請者別、CSV 出力）、経費専用の追加読取（参加人数・区間・食い違いに印を付けて人に確かめさせる）、交通費の照合（運賃マスタと通勤定期区間）、規程のヒアリング（文書や質問への回答から規程の変更案を作り、選んだ変更だけ保存）。
- **どれも使うときに設定すればよく、設定しなければ今までと同じ判定・画面のまま**使える。規程タブの先頭に「実用機能の準備」カードを出し、何が設定済みかと台帳への導線を示す。
- **口座番号は保存時に封緘**し（アプリの鍵。SQLite に平文で置かない）、画面・API・ツールには末尾 4 桁だけを出す。平文を含む口座つき CSV と振込ファイルは承認権限 + 監査ログのときだけ出す。
- 振込データは**受取人名が変換後 30 バイトを超えたら切り詰めずに作成を止める**（黙って切ると別人の名義になりうるため）。中点など変換できない文字も止めて、直す場所を示す。
- エージェントから呼べる**読み取り専用ツールを 4 本**追加: `expense_summary`（集計）/ `expense_advances`（仮払）/ `expense_card_transactions`（カード明細と照合状況）/ `expense_fares`（運賃マスタ）。口座と領収書の画像は返さない。
- 理由コードは 27 件から 43 件になった。サンプル（従業員・組織・承認経路・カード明細・運賃・振込ファイルの期待バイト列など。すべて架空）を `samples/expense/` に追加。
- API: `/expense/*` に 65 本（`docs/04-api-spec.md` §3.5）。DB スキーマは **version 9**（従業員・仮払・カード・振込・ヒアリングなどのテーブル。既存の申請はそのまま読め、派生の索引は移行時に埋め戻す）。詳細は [docs/21-expense.md](docs/21-expense.md) §20 / [ADR-0043](docs/adr/0043-expense-practical-extensions.md)。

### 追加 — 業務テンプレートに「経費精算」「請求書発行と入金消込」「契約書レビューと期限台帳」を追加した（2026-09-15）

- **業務テンプレート一覧**（左ナビ「作る」→ 業務テンプレート）から、仕訳と並んで 3 つ選べるようになった。
- **業務テンプレートは実験的な機能として、初期状態では非表示にした。** 設定画面の「実験的な機能 → 実験的な機能を表示する」をオンにすると左ナビに現れる（このブラウザに保存）。非表示のまま `#/journal` などの直リンクを開くと、理由と「設定を開く」ボタンを出す。
- **業務テンプレートは実験的な機能だと画面で示すようにした。** 左ナビの「業務テンプレート」に「β」、一覧の見出しと各カードに「実験的」の印、一覧と各業務画面の先頭に注意書きを出す。画面・保存データの形式・判定のルールが変わりうること、結果は人が確認し会計・税務・法務の最終判断に使わないことを伝える。
  - **経費精算**: 領収書の画像 / PDF / CSV から明細を起こし、ワークスペースの規程（費目・上限額・証憑や登録番号などの必須項目）と自動で突き合わせて合否（合格 / 要確認 / 差し戻し）を判定する。確認・差し戻し・承認（自己承認は禁止できる）・仕訳下書きの作成・精算 CSV の出力まで一通り行える。
  - **請求書発行と入金消込**: 取引先・請求書を管理し、税率別の端数処理まで含めた税額計算で発行する。銀行 / カード明細 CSV を取り込むと、金額と振込人名から入金消込の候補を自動判定（振込手数料の差引・複数請求書の合算・部分一致名にも対応）し、確定すると仕訳を起票する。
  - **契約書レビューと期限台帳**: 自社の審査基準（playbook）を登録し、契約書（テキスト / 画像 / スキャン PDF）を取り込むと条項を抽出して基準と突き合わせ、受け入れ可 / 要交渉 / 不可 / 要確認を判定する。締結登録すると更新・満了・通知期限を期限台帳で追跡できる。レビュー結果には「審査基準との照合であり法的判断ではない」という注意文言を必ず添える。
- **エージェントから呼べる読み取り専用ツールが業務ごとに 3 本ずつ増えた**（保存・承認・出力はわざとツール化していない。帳簿・契約の状態を変える操作はこれまでどおり人が画面で行う）。
  - 経費精算: `expense_check_receipt`（添付したレシートを規程と突き合わせて合否を返す）/ `expense_claims`（申請の一覧と承認状況）/ `expense_policy`（規程の参照）
  - 入金消込: `receivables_outstanding`（未回収の請求書）/ `receivables_match_candidates`（入金明細の消込候補と理由）/ `receivables_invoice_draft`（添付した注文書・見積書から請求書案を作る）
  - 契約: `contract_review_draft`（添付した契約書のレビュー案）/ `contract_deadlines`（期限台帳）/ `contract_clauses`（条項の横断検索）
- **チャット / エージェント実行にテキスト添付（`documents`）が増えた。** ブラウザで PDF のテキスト層から抜いた本文などを、画像（最大 2 枚）とは別に最大 2 件・合計 40 万字まで渡せる。本文はモデルへのメッセージには載せず、ツールの実行文脈にだけ渡す（12B 級モデルの文脈をメッセージ本文で埋めないため）。
- 試すためのサンプルデータ（すべて架空）を `samples/expense/`・`samples/receivables/`・`samples/contract/` に追加した。
- API: `/expense/*`・`/receivables/*`・`/contracts/*`（`docs/04-api-spec.md` §3.5〜§3.7）、`GET /runtime/capabilities` の `expense` / `receivables` / `contract`。DB スキーマは version 6（経費精算）・7（入金消込）・8（契約）を追加（既存データはそのまま）。

### 変更 — 業務テンプレートを増やしやすくする登録点を整理した（2026-09-15）

- 仕訳 1 つのために共有ファイルへ直書きされていた配線（ルート・認可・エラー写像・スキーマ・ETL ノード・組込みツール・マイグレーションなど）を、業務ごとのファイルへ分離した。共有ファイルは業務ごとのファイルを並べて呼ぶだけになり、次の業務を追加するときに他業務のファイルへ触れずに済む（[ADR-0039](docs/adr/0039-business-feature-registration.md)）。仕訳の挙動・API・DB スキーマは変えていない。

### 修正 — 仕訳: 帳票を添付し忘れたとき、エージェントが黙って「何も無い」と答えていた（2026-09-14）

- 添付が無いまま仕訳ツールを呼ぶと、**空の結果が返って成功扱い**になっていた。エージェントからは「帳票に何も
  書いていない」ように見えるため、「画像を添付してください」と案内できなかった。添付が無いことを伝えて
  落とすよう直した。エージェントはその理由を読んで、利用者に何をすればよいか返せる。
- 追加した E2E（仕訳ツールを組み込んだエージェントを実際に動かす通し試験）で見つけた。

### 追加 — 仕訳: 添付したレシートを「どう仕訳するか」までエージェントが答えられるようになった（2026-09-14）

- **仕訳画面の 取込 → 判定 の流れが、そのまま 1 本のツールになった**（`journal_draft_entry`）。チャットで帳票を添付して
  「これどう仕訳する？」と聞くと、読み取って自分のルールで判定し、借方・貸方・科目・税区分・金額まで返す。
- **ルールに当たらなかったときは、その理由を返す。** ルールが無い、候補が複数ある、必要な項目が読めていない、といった
  理由をそのまま返すので、次に何を直せばよいかが分かる。黙って空の結果を返さない。
- **保存はしない。** 提案するだけで、帳票も仕訳も作らない。帳簿に残すのはこれまでどおり画面から人が押す。
- 読み取った事実だけが欲しいときは、これまでどおり `journal_read_attachment` を使う。

### 追加 — 仕訳: チャットに添付したレシートをエージェントが読めるようになった（2026-09-14）

- **チャットで帳票の画像を添付して聞けるようになった。** 「このレシート、いくら？」「どう仕訳する？」と尋ねると、
  エージェントが組込みツール（`journal_read_attachment`）で添付を読み、発行者・登録番号・日付・合計・税率別の内訳を返す。
  取込タブと同じ読み取りを使う。
- **読むだけで、保存はしない。** 帳票も仕訳も作らない。仕訳として残すときは、これまでどおり取込タブから取り込んで判定する。
- **読めなかった項目は空で返す**。書いていない項目があるのは普通のことなので、埋めずに空にする。読み取りの申告（税率別合計が
  合わない等）も一緒に返すので、鵜呑みにせず確かめられる。
- **添付が無いときは、その旨をはっきり返す。** 「何も写っていない」と誤解させないため、空の結果ではなく理由を返す。
- 対象は画像（JPEG / PNG / GIF / WebP）。PDF は取込タブと同じく、画像に変換してから添付する。

### 追加 — 仕訳: いまの絞り込みをそのままエージェントのツールにできるようになった（2026-09-14）

- **「出力」タブに「この条件をツールにする」を追加した。** 状態と期間で絞り込んだ状態のまま名前を付けて保存すると、
  エージェントが呼べる読み取り専用ツールになる。**用途ごとに何本でも作れる**（月次の消耗品費、部門別、未確定の確認など）。
  作ったツールはエージェント画面で組み込む。
- **条件はツールに焼き込むので、引数は無い。** 組込みの `journal_entries` は期間と科目を引数で受けるが、こちらは
  条件が決まっているぶんモデルが取り違えにくい。名前は関数名の規則へ整形し、整形された場合は保存前に呼ばれる名前を見せる。
- **読むだけ。** 判定（帳票から仕訳を起こす）と出力（CSV を作る）は、これまでどおりツールにしていない。どちらも帳簿の
  状態を変えるため、画面から人が押す。

### 変更 — 業務向けの機能を「業務テンプレート」にまとめた（2026-09-14）

- **左ナビの「仕訳」が「業務テンプレート」に変わった。** 開くと業務の一覧が出るので、そこから仕訳を選ぶ。
  仕訳だけが特定業務向けで、データソース・ツール・エージェントのような全体で使う機能と粒度が揃っていなかったため、
  入れ物を 1 つ設けてその下にまとめた。今後増える業務向けの機能も同じ一覧に並ぶ。
- **画面そのものは変えていない。** `#/journal` の直リンク、他画面からの「仕訳を開く」導線、画面内の 5 ステップはそのまま。
  仕訳画面の左上に「← 業務テンプレート」を置いたので、一覧へ戻れる。
- 一覧に出るのは**今すぐ使えるものだけ**。開けない「準備中」の項目は並べない。

### 追加 — 仕訳: 弥生 / freee / マネーフォワードの形式で書き出せるようになった（2026-09-14）

- **会計ソフトにそのまま取り込める CSV を出せるようになった。** 「出力」タブの形式で 弥生会計 / freee / マネーフォワード を選べる。
  これまでの汎用 25 列も従来どおり選べる。各社の列の並びと束ね方（弥生の識別フラグ、freee の `[表題行]` / `[明細行]`、MF の取引No）は
  各社の仕訳インポート仕様に合わせてある。
- **弥生は Shift-JIS で書き出す。** UTF-8 のままだと弥生の取込画面で文字化けするため、ダウンロードするファイルだけ Shift-JIS にする。
  画面のテキストエリアには読める文字のまま出るので、中身の確認はそのままできる。
- **税区分の名前は自分のマスタから引く。** 各社の税区分名はコードに持たず、「科目」タブの税区分に登録した対応名を使う。
  名前を変えれば出力もそれに追従する。
- **書き出せなかったところは黙って埋めない。** 税区分の対応名が無い、摘要が長すぎて切り詰めた、伝票番号にできる数字が無い、
  1 伝票の行数上限を超えた、Shift-JIS に無い文字が混ざっている、のいずれかがあれば、理由と直し方を出力結果の下に一覧で出す。
- API: `GET /journal/export` の応答に `encoding` / `contentBase64?` / `warnings[]` が増えた（`content` はこれまでどおり読める本文）。

### 追加 — 仕訳: 確定した仕訳をエージェントに聞けるようになった（2026-09-14）

- **チャットで「9 月の消耗品費はいくら？」と聞けるようになった。** エージェントが確定済みの仕訳を読む組込みツール（`journal_entries`）を追加した。1 行 = 1 仕訳行（借方 × 貸方）で、日付・科目・税区分・金額・摘要・インボイス区分・元の証憑 id を返す。期間（`2026` や `2026-09`）と科目名（部分一致・借方でも貸方でも）で絞り込め、どちらも省略できる。
- **見せるのは確定済みだけ。** 下書き（draft）の仕訳は返さない — まだ帳簿に載っていないものを集計に混ぜないため。科目名は**今のマスタ**の名前で返すので、改名しても最新の名前で読める。
- **エージェントは仕訳を作れないし、出力もできない。** 判定（証憑から仕訳を起こす）と CSV 出力は**わざとツールにしていない**。どちらも帳簿の状態を変えるため、これまでどおり画面から人が押す（`docs/20-journal.md` §14.1）。
- Tool Builder のノードに **仕訳（`journal-entries`）** が加わった。自分のツールでも仕訳を入力に使える（`docs/06-etl-tool-builder.md` §2.1）。

### 追加 — 仕訳: 帳票の読み取りとヒアリング（2026-09-13）

- **レシートや請求書の画像・PDF・メール本文を読み込んで、仕訳の元になる項目を自動で埋められるようになった**（取込タブ）。発行者・登録番号・日付・合計・税率別の内訳・明細を読み取り、和暦は西暦へ、全角やカンマ付きの金額は整数へ、登録番号は `T` + 13 桁へそろえる。**読み取った結果はその場で保存されない** — 画面で確認・修正してから保存する。
- **読み違えを黙って通さない。** 読み取り後に突き合わせを行い、おかしなところを警告として一覧に出す: 税率別の合計が総額と合わない（差額つき）、明細の合計が合わない、単価 × 数量が金額と合わない、お預り − お釣が合計と合わない、登録番号が 13 桁でない（読み取れた文字列をそのまま添える）、同じ帳票で税込と税抜が混ざっている、記載の無い 0 円の税率行、8% の明細に軽減税率の記号が無い、請求書なのに登録番号が無い、日付が読めない。**値を勝手に直すことはしない**（帳簿の数字を推測で書き換えないため）。項目ごとの信頼度と読み取り元の文字列も返すので、あやしい項目から確認できる。
- **確定できなかった証憑を、対話でルール化できるようになった（Stage 2 ヒアリング）。** 「該当ルール無し」などの理由・帳票の内容・当てはまる「迷うケース」（飲食の目的、EC で買ったもの、固定資産の確認、家事按分、源泉徴収、経過措置 …）・**自分の科目マスタ**をもとに、一度に最大 3 問の質問が出る。答えると次の質問か、**新しいルールと今回の仕訳の提案**が返り、確認して登録すると再判定されて仕訳の下書きができる。中止すればその証憑は未判定へ戻る。
- **提案が科目マスタを勝手に変えることはない。** 新しい科目・税区分・補助科目が提案されても、実際に登録されるのは**利用者がチェックした項目だけ**。提案されていない項目は登録できない。提案されたルールと仕訳はサーバー側で検証し（科目や税区分が実在するか、貸借が一致するか、条件が正しい形か）、壊れていれば 1 度だけ直させ、それでも駄目なら**提案として保存せず**理由を表示する。ルールと仕訳は登録前に画面で編集でき、編集後も同じ検証を通る。
- **モデルが無い・力不足なら、その場で理由が分かる。** 画像を読むには vision 対応のモデルが要る。設定が無いときは「設定画面のモデルを保存してほしい」「テキストを貼り付ける手もある」と伝える 409 を返し、ヒアリングの導線も出さない（`GET /runtime/capabilities` の `journal.hearing.enabled` は構造化出力の可否に連動するようになった）。読み取りは 1 枚あたり数十秒〜数分かかることがあるので、画面を閉じたり中止したりすればモデルの呼び出しごと止まる。
- API: `POST /journal/documents/extract`、`POST|GET /journal/hearings`、`GET /journal/hearings/{id}`、`POST /journal/hearings/{id}/answers|accept|cancel`（`docs/04-api-spec.md` §3.4、`docs/20-journal.md` §6 / §7）。参照は `workspace:read`、変更は `workspace:edit`（受け入れは監査対象）。エラーは `409 JOURNAL_EXTRACTION_UNAVAILABLE`（モデル未設定・能力不足）と `502 JOURNAL_EXTRACTION_SCHEMA`（応答が直らない）。DB スキーマの変更は無い。

### 追加 — 仕訳（2026-09-13）

- **伝票・帳票から仕訳を起こす「仕訳」機能を追加した**（サイドバー「作る」の仕訳タブ）。銀行・カードの明細 CSV や手入力の帳票を取り込み、自分で作ったルールで自動的に仕訳を起こし、会計ソフトへ渡す CSV として書き出せる。
- **勘定科目・税区分・補助科目・部門は、はじめから自分のものにできる。** 標準セット（青色申告決算書の科目 + 会計ソフトの慣用科目 + 仕訳の相手科目、消費税の税区分）は**初期値に過ぎず**、追加・改名・無効化・並び替え・CSV での取込と書き出しができる。科目を消しても過去の仕訳は当時の名前のまま残り、ルールは科目を内部 id で覚えているので改名しても効き続ける。「標準に戻す」もいつでも押せる。
- **判定は 2 段階。** ルールに当てはまれば仕訳の下書きを自動で作り（Stage 1）、当てはまらなければ**なぜ確定できなかったか**を理由つきで示す（該当ルール無し / 複数ルールの競合 / 必要な項目が足りない / 金額が大きいので確認が要る / 科目がマスタから消えている / 貸借が合わない）。同じ入力からは必ず同じ仕訳が出るので、帳簿として使える。理由ごとに次に何をすればよいかが分かるようになっている（ヒアリングによるルール化はフェーズ 2）。
- **銀行・カードの明細 CSV をそのまま読み込める。** 楽天銀行・三菱UFJ銀行・三井住友銀行・ゆうちょ銀行・楽天カードと汎用形式（日付,摘要,出金,入金,残高）を列名から自動で判別し、当てはまらない CSV は列を自分で対応づけられる。**1 行読めなくても取込全体は止まらない** — 合計行や注記行は「取り込めなかった行」として行番号と理由つきで一覧に出る（行番号は表計算ソフトの行番号と同じ）。
- **仕訳 CSV を書き出せる。** 25 列の汎用形式（UTF-8 BOM・CRLF・税込整数）で、状態や期間で絞り込める。「出力済みにする」を選べば、同じ仕訳を二重に会計ソフトへ入れずに済む。弥生・freee・マネーフォワードの各形式は列の対応表だけ用意してあり、変換はフェーズ 3。
- **仕訳を消しても帳簿の辻褄が合う。** 証憑を削除すると自動で作られた下書きの仕訳も消え（確定済みの仕訳は残る）、仕訳を削除すると元の証憑は未判定に戻る。確認済み・確定済みの仕訳は、ルールを直して再判定しても**上書きされない**。
- API: `/journal/chart`（科目マスタと CSV 取込・出力）、`/journal/rules`（ルール CRUD と「文書でテスト」）、`/journal/documents`（文書 CRUD・CSV 取込・判定）、`/journal/entries`（仕訳 CRUD と確定）、`/journal/export`、`/journal/csv-presets`、`GET /runtime/capabilities` の `journal`（`docs/04-api-spec.md` §3.4、[ADR-0038](docs/adr/0038-journal-two-stage-judgment.md)）。参照は `workspace:read`、変更は `workspace:edit`。DB スキーマは version 5（`journal_*` 5 テーブルの追加のみ。既存データはそのまま）。

### 追加 — LLM 判定を基準別に（根拠先出し・判定契約・軌跡・自己一貫性）（2026-09-13）

- **judge 指標の判定が基準別になった。** 判定者はルーブリックの基準ごとに「理由 → 段階スコア」を返し、サーバーが重み付きで合成する（判定できない基準は `null` で分母から外れる。全基準が判定不能なら `JUDGE_UNASSESSABLE` として欠損）。実験結果には合成スコアに加えて基準別スコアが `<指標>:<基準>` の名前で並ぶので、実験比較や品質ゲートで「どの基準が退行したか」を追える。判定レコードには基準ごとの理由が残る。
- **判定の偏りを抑える。** プロンプトは各基準で理由を先に書かせ、「長さ・体裁・断定口調・専門用語それ自体に点を付けない」「基準は独立に判定する」を明示する。出力が契約（スキーマ・基準の過不足・段階に無い値）に合わなければ違反を示して 1 回だけ修正を求め、それでも合わなければ `JUDGE_SCHEMA`。
- **判定契約とコストを記録する。** 各判定レコードに `contract`（プロンプト版・ルーブリック版の指紋）と `usage`（使ったトークン）が付く。判定者側の更新でスコアが動いたときに切り分けられる。
- **判定者にツール呼び出し列と会話履歴を渡せる。** ルーブリックに `tracePolicy`（optional / required / forbidden、既定 optional）を追加。turn 事例では Run のツール呼び出しと結果（先頭 10 行）、scenario 事例では最終応答より前の会話を、命令として扱わない隔離ブロックの中で判定者に見せる（上限 20 件・各 2,000 文字）。`required` で軌跡が無い事例は `JUDGE_INPUT` として欠損になる。
- **自己一貫性（`judgeSamples`）。** 実験の起票で `judgeSamples`（1〜5、既定 1）を指定すると判定を独立に複数回行い、中央値を採用してばらつき（`dispersion`）を記録する。範囲が 0.25 以上なら `uncertain` として「判定が割れている」と示す。一部のサンプルが失敗しても残りで集約する。
- **judge が未設定のまま judge 指標つきの実験を起票できなくなった。** これまでは起票が通って全事例が `EVALUATION_DOMAIN: … model.model must be a non-empty string` で失敗し、原因（judge スロットにモデルが無い）に辿り着けなかった。起票は `409 JUDGE_MODEL_NOT_CONFIGURED` で止まり、`GET /runtime/capabilities` の `judge: { configured, provider?, model? }` で実験画面が起票前に未設定を示せる。起票後に設定が消えた場合も、事例を落とさず判定レコードを `JUDGE_PROVIDER`（直し方つき）の失敗にする。
- **`tracePolicy: required` のルーブリックを scenario 事例に使う矛盾を起票時に止める。** scenario 事例は軌跡を持たないので全事例が `JUDGE_INPUT` になっていた。`409 JUDGE_TRACE_UNAVAILABLE` として、直すべきルーブリックを本文の `rubric: { id, version }` で返す（`docs/19-troubleshooting.md` §5）。
- API: `POST /judge-rubrics` の `tracePolicy`、`POST /experiments` の `judgeSamples`、判定レコードの `criteria` / `samples` / `dispersion` / `uncertain` / `usage` / `contract`（`docs/04-api-spec.md` §3.3、[ADR-0037](docs/adr/0037-criterion-level-judging.md)）。保存済みのルーブリック・実験・結果はそのまま読める。

### 修正 — 壊れたリクエスト本文が 500 になっていた（2026-09-12）

- **不正な JSON・空の本文・上限（10 MiB）超えの本文は、Fastify が返す本来の 400 / 413 で返るようになった。** これまではカスタムのエラー処理がこれらを 500「サーバー内部でエラー」に変えていて、送った内容が壊れていることに気づけなかった。あわせて 500 はサーバーログにスタック付きで記録する（原因追跡ができるように）。

### 追加 — ツール検証: 異常系ケースと AI によるケース案（2026-09-12）

- **「失敗すること」を期待する異常系ケースを書けるようになった。** 期待に「結末」（成功 / エラー）を追加し、「エラー」を選ぶと、引数不正やノードのエラーで実行が失敗したときにそのケースは**合格**になる（失敗理由はそのまま表示される）。失敗するはずが成功してしまったら不合格になり、残りの期待も評価して実際に何が起きたかを見せる。既存のケースは影響を受けない（結末を指定しなければ従来どおり）。
- **ツール検証の画面から、AI に 正常 / 境界 / 異常 のケース案を作らせられる**（分析アシスタントと同じモデル設定を使う。モデル未設定なら機能は出ない）。ツールの引数定義・出力・グラフの要約と、設計時サンプルでの実行結果をもとに、カテゴリごとに 1〜5 件の引数と期待を提案する。提案は保存されず、レビューして実行・保存する。モデルの出力はサーバーで検証し、定義に無い引数の除去・型の変換・不正な期待の除去を行って、その内容を各案の注意として表示する。
- API: `POST /tool-checks/suggest`、`GET /runtime/capabilities` に `toolCheckSuggestions.enabled` を追加、`expectations.outcome`（`docs/04-api-spec.md` §3.2）。提案は `tool:execute`。

### 追加 — ツール検証（2026-09-12）

- **保存したツールを、エージェント抜きで単体テストできる「ツール検証」画面を追加した**（サイドバーの「確かめる」グループ）。ツールを選び、エージェントが渡すのと同じ形の引数を入れて実行すると、出力テーブル・ノードごとの行数・所要時間が見える。実行は Agent 経路と同じ規則で引数を束縛し、全データで計算する（表示だけ 100 行に絞る）。セッション成果物への書き込みなどの副作用は起こさないので、`write` のツールも安全に試せる。
- **期待を書いて合否を出せる。** 行数（== / >= / <=）・必ず含まれる列・セル値（== / != / >= / <= / 部分一致、「1行でも」「全行」）・所要時間の上限。引数不正やノードのエラーは「失敗」ではなく「エラー」として理由（どのノードか）ごと表示する。
- **ケースとして保存し、あとでまとめて再実行できる。** 名前・引数・期待・（任意で）ツールの版を保存し、「すべて実行」で回帰検証になる。各ケースは直近の結果（合格 / 不合格 / エラーと要約）を一覧に持つ。版を固定したケースのツールが消えていても、一括実行は止まらずそのケースだけエラーになる。
- API: `POST /tool-checks/run`、`GET|POST /tool-checks/cases`、`DELETE /tool-checks/cases/{id}`、`POST /tool-checks/cases/{id}/run`、`POST /tool-checks/cases/run-all`（`docs/04-api-spec.md` §3.2）。参照は `tool:read`、実行は `tool:execute`、保存・削除は `tool:edit`。DB スキーマは version 4（`tool_check_cases` テーブルの追加のみ。既存データはそのまま）。

### セキュリティ — MCPサーバー設定の権限と起動コマンドの既定（2026-09-03）

- **MCPサーバー設定の保存・置換・削除・接続テストに `operate` 権限（operator / workspace-admin）が必要になった。** stdio の設定はサーバーホスト上で子プロセスを起動する設定で、保存できる主体はホスト上で任意コードを実行できる。これまでは editor（`roles` を書かないトークンの既定）でも設定できた。一覧の参照は従来どおり全ロール。単一ユーザーモードは全ロールを持つので変わらない。MCP 画面は権限が無いとき保存・テスト・削除・適用のボタンを無効化し、必要なロールを表示する。
- **起動を許すコマンドの既定を `npx, uvx, bunx, cmd` に絞った**（`node` / `python` / `python3` / `uv` / `bun` / `deno` / `docker` を外した）。**更新前に `node …` などで保存した設定は接続時に止まる。** エラーメッセージに出る `AGENTCONTEXT_MCP_ALLOWED_COMMANDS=…` をサーバーの環境変数に設定して再起動すれば従来どおり動く（`docs/19-troubleshooting.md` §10.1）。`cmd /k` は形ごと拒否するようにした。
- **子プロセスの実行内容を差し替える環境変数（`PATH` / `NODE_OPTIONS` / `LD_PRELOAD` / `PYTHONPATH` など）を `transport.env` で上書きできなくした。** `AGENTCONTEXT_MCP_ALLOWED_COMMANDS=*` でも拒否する。

### 修正 — 中止が効かない・集計が黙って狂う（2026-09-03）

- **マルチエージェント実行の中止が実際に実行を止め、状態が巻き戻らなくなった。** これまでは「中止」を押しても参加エージェントは最後まで走り、次のイベント保存で状態が running に戻って最終的に succeeded になっていた。実行中の中止は参加者へ即時に中断を伝え、保存は「まだ running のときだけ」の条件付きにした。終端状態（成功・失敗・中止）の実行を再度中止しても何も変わらない（200 で現状を返す）。
- **Agent Factory の中止が生成処理まで届き、行が running のまま残らなくなった。** ツール/スキル/エージェント生成・検証・改善の各段階へ中断シグナルを配線し、保存を条件付きにした。ワーカー停止で中断された実行は cancelled として記録される（次回起動まで running のまま固まらない）。
- **ツールの実行は常に全行で計算する。** これまでプレビューの行数上限（実行時は 100 行）が各ノードの出力を切り詰めて下流へ渡していたため、100 行を超えるデータでは集計・並べ替え・結合の結果がエージェントの答えごと黙って狂っていた。行数上限はプレビュー表示のスナップショットだけに効き、計算には**実行上限 250,000 行**（超過はエラー）と結合の 100,000 行ガードが効く。エージェントへ返す出力が容量で切られたときは「N 行のうち M 行を表示、K 行省略」を結果に添える。ツールビルダーのプレビューは「全 N 行のうち 100 行を表示」と実件数を示す。
- **時系列分析の欠損補完に 100,000 バケットの上限を設けた。** 数年に散らばる数行を分単位で補完すると 1 千万行・数 GB を生成してサーバーが 100 秒以上止まっていた。超過時は「期間を狭めるか粒度を粗く」のエラーになる。あわせて分単位の集計で時刻の「時」が 0 に潰れていた不具合を修正。

### 変更 — モデル設定の簡素化（2026-07-28）

- **プロバイダの選択肢を主要どころだけに絞った。** これまでは登録簿の138プロバイダが並んでいた。いまは **OpenAI / Anthropic / Google**（直接API）、**Microsoft Azure AI Foundry / AWS Bedrock / Google Cloud Vertex AI**（OpenAI互換エンドポイント。ベースURLの雛形が入る）、**OpenAI互換エンドポイント**（LM Studio・vLLM ほか）の7つ。一覧に無いサービスも、OpenAI互換であればベースURLを入れれば従来どおり使える。
- **モデル名のプルダウンをやめ、入力欄にした。** モデルは提供元で頻繁に入れ替わり、Azure・Bedrock・Vertex では自分でデプロイ・有効化したものしか使えないため、こちらが固定の一覧を持っていても正しくならなかった。代わりに提供元のモデル一覧ドキュメントへのリンクを添える。OpenAI互換エンドポイントでは「モデル一覧を取得」で**実際に問い合わせた**候補を入力補完として出す。
- 「ソース（プロバイダレジストリ / OpenAI互換エンドポイント）」のラジオボタンを廃止した。接続方式は選んだプロバイダから決まる。
- 以前に保存した設定はそのまま動く。絞り込みで一覧から外れたプロバイダも、保存済みであれば選択肢に残す。

### 追加 — はじめての利用体験（2026-07-28）

- **サンプルデータを画面から投入できるようになった。** 「チャット」画面のウェルカムカードの「サンプルを読み込んで試す」、または「データソース」画面の「サンプルを読み込む」を押すと、データソース・ツール・スキル・Wiki・エージェントが繋がった一式が入る。何度押しても重複しない。
- **画面ごとのヘルプを追加。** 左ナビ下部の「? ヘルプ」で、いま開いている画面の説明と関連ドキュメントが出る。全13画面に対応。
- **実行中のチャットを中断できるようになった。** 送信ボタンが停止ボタンに変わり、押すと実行を止めて入力内容を入力欄へ戻す。経過時間と、10秒・30秒・2分の節目での状況説明も表示する。
- **エラー文言を日本語化し、「次に何をすればよいか」を添えた。** モデル未設定・接続失敗・認証失敗・タイムアウト・ツール呼び出しの失敗・上限到達・ETLのスキーマ不一致など。
- 空状態から関連画面へ移動できるリンクを各画面に追加。

### 追加 — 運用（2026-07-28）

- **バックアップと復元。** SQLite のオンラインバックアップでスナップショットを取り、セッション成果物と一緒に1つのディレクトリへ出力する。「ステータス」画面から、または `npm run backup` で実行できる。復元はサーバー停止中のCLI専用。暗号鍵は既定で含めない（引っ越し時のみ `--include-secret-key`）。
- **本番起動に対応。** `npm run build` → `npm start` で、UIとAPIを同じポートから配信する。`GET /health`（liveness）と `GET /ready`（readiness）を追加。graceful shutdown で実行中のジョブを既定10秒まで待つ。
- **中断された実行の復旧と、保持期限（retention）の定期掃除。**
- CI パイプライン、トレース、ロギングを整備。

### 追加 — 編集の安全性（2026-07-27）

- **下書きの自動保存。** ツール・スキル・エージェント・マルチエージェントの編集内容をブラウザへ自動保存する。次に開くと「未保存の編集内容があります」バナーから「復元」「破棄」を選べる（勝手には復元しない）。
- **未保存のまま画面を離れようとすると確認ダイアログが出る。**
- **URLで画面を指せるようになった**（`#/tool`、`#/agent` など）。ブラウザの戻る・進むが効き、ブックマークできる。

### 追加 — モデルプロバイダの選択（2026-07-27）

- **画面からモデルを設定できるようになった。**「設定」画面の「モデルプロバイダ」で、プロバイダ・モデル・APIキーを指定して保存する。それまでは環境変数だけだった。
  - **プロバイダレジストリ**（OpenAI 等・カタログからモデル一覧を取得）と **OpenAI互換エンドポイント**（LM Studio 等・「モデル一覧を取得」で列挙）を選べる。どちらもモデル名の手入力に切り替えられる。
  - 用途別に **メインモデル**（エージェント実行）と **評価モデル**（LLM採点）の2スロット。
  - 「テスト」ボタンで接続を確認できる。
- **APIキーを暗号化して保存する。** 平文でDBに置かず、ブラウザへも返さない（末尾4文字のみ表示）。鍵は `~/.agentblume/secret.key`。
- モデル通信を Mastra の model router へ委譲し、プロバイダを実行時に切り替えられるようにした。

### 追加 — Agent Factory の強化モード（2026-07-27）

- **既存エージェントを強化できるようになった。** それまでは新規生成のみだった。「どう改善したいか」を書くと、既存の資産を土台に差分を積む。
- **システムプロンプトの扱いを選べる。**「既存プロンプトを保つ（ツール・スキルのガイドのみ更新）」が既定で、手書きの文言を勝手に書き換えない。「モデルに役割・ルールを書き直させる」も選べる。
- 失敗した実行を同じ入力で再実行できる「再実行」を追加（2026-07-26）。

### 追加 — 保存済み資産の一覧画面（2026-07-21 / 2026-07-24）

- **ツール・スキル・エージェント・マルチエージェントの各画面が2層になった。** まず一覧が開き、「開く」で編集画面へ、「新規作成」で新しい定義へ進む。行からの論理削除にも対応。
- それまでは内部IDを手で入力しないと保存済みの定義を開けなかった。

### 変更 — 用語（2026-07-26）

- **UI表記を「Harness」から「マルチエージェント」（英語UIでは `Multi-Agent`）へ統一した。**
- あわせて、**エージェント1件の実行時機能トグルを「実行オプション（Runtime options）」へ改名**した。同じ「ハーネス」という語が2つの別概念に使われていて紛らわしかったため。
- 設計文書・型名・API パス（`AgentHarness` / `/harnesses`）と画面ID・URL（`#/harness`）は変更していない。

### 追加 — 会話と実行の質（2026-07-25 / 2026-07-26）

- **チャットが会話履歴を保持するようになった。** それまでは毎回1往復で完結していた。
- ローカルモデル向けのストリーミングを適応的にし、応答の取りこぼしを減らした。
- 現在日時を返す組み込みツールを追加。
- エージェントの引数（Agent Input）とフィルタ条件の扱いを改善。
- UXの安定性、ETLエンジンの堅牢性、開発スクリプトのプロセス管理を全面的に見直した。

### 追加 — MCP（2026-07-26）

- **MCPサーバーの設定と、その定義のシリアライズに対応。** 設定したMCPサーバーのツールをエージェントから呼べる。

### 追加 — Agent Factory（2026-07-20）

- **「やりたいこと」とデータソースから、ツール・スキル・エージェント・検証資産（ペルソナ・シナリオ）一式を自動生成し、疑似ユーザー検証の結果から自動で改訂を繰り返す機能。**
- 計画承認を必須にするオプション、実行タイムライン、イテレーション別メトリクスのレポートを含む。
- 生成物はすべて draft のまま置かれる。昇格は検証画面の品質ゲートから人が行う。

### 追加 — マルチエージェント（2026-07-16 〜 2026-07-21）

- **保存済みエージェントを型付きの連携パターンへ組み立てられるようになった** — Agent as tools / Sequential / Concurrent / Handoff / Group Chat / Magentic。
- スロットにはエージェントの**バージョンを固定**して割り当てるので、後から新版を作っても既存の動作は変わらない。
- Handoff の会話再開、Magentic の計画承認、24時間保持される checkpoint に対応。
- Concurrent の集約方法（機械的に連結／投票で決定／Agentで統合）と参加者数の増減を画面から選べる。
- パターンごとに異なる構造図でキャンバスを描画。
- **チャットに画像を添付できるようになった**（PNG / JPEG / WebP / GIF、最大2枚・各3 MiB）。

### 追加 — 分析ノードとデータソース（2026-07-12 〜 2026-07-14）

- ETL の分析ノードを追加 — グループ集計・基本統計量・相関分析・時系列分析・外れ値の検出／除外。
- 出力ノードを追加 — ワークスペース出力・グラフ出力・チャート出力。
- **データソースを永続化し、CSV/JSON のアップロードと読み取り専用のDB接続に対応。**
- 検証画面にバッチ実験タブと品質ゲートタブを追加。

### 追加 — 検証と記憶（2026-07-05 〜 2026-07-08）

- **長期記憶（Wiki）。** ページの編集・検索、エージェントへのアタッチ、実行からの記憶提案（蒸留）とレビュー。
- **疑似ユーザーによるシナリオ検証。** ペルソナ × 複数ターン会話 × アンケート。
- **サブエージェント委譲**（Agent-as-Tools）。
- Mastra Evals によるエージェント応答の評価。
- 実行を観測する「動作確認（Inspector）」画面。
- ETL の変換ノードを拡張 — 結合・縦結合・並べ替え・重複排除・null処理・値置換。
- **UIの日英切り替え。**

### 追加 — 基盤（2026-07-02 〜 2026-07-04）

- ETL ツールエンジンとツールビルダーUI。
- ツール・エージェント・スキルのバージョン付き永続化。
- エージェントのツール呼び出しと実行トレースの永続化、構造化出力。
- Fastify による HTTP API 層。
- チャット・MCP・検証・設定の各画面。
- 開発用の起動スクリプトと LM Studio 設定スクリプト。

---

## 記録の方針

- 内部構造の変更・テスト追加・依存更新は、利用者の操作や結果が変わらない限り載せない。
- 破壊的変更（保存形式・API・環境変数の非互換な変更）は必ず載せ、移行手順を添える。
- 日付は変更が入った日であり、リリース日ではない。